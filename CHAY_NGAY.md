# 🚀 CHẠY NGAY - 1 LỆNH DUY NHẤT

## ⚡ Chỉ cần chạy:

```bat
run.bat
```

## ✨ Tự động 100% - Không cần làm gì!

Script sẽ **TỰ ĐỘNG**:
- ✅ Tìm Python 3.11/3.12
- ✅ **Cài Python 3.11 nếu không có** (silent, không cần click gì!)
- ✅ Tạo virtual environment
- ✅ Cài tất cả dependencies
- ✅ Cài webview
- ✅ Chạy app với auto-login

## 🎯 Cài Python 3.11 tự động qua:

1. **Winget** (Windows Package Manager) - Ưu tiên
2. **Chocolatey** (nếu đã cài)
3. **Download trực tiếp** từ python.org

**Tất cả đều SILENT - không cần tương tác!**

## 📋 Yêu cầu

- Windows 10/11
- Internet (lần đầu)
- **KHÔNG CẦN** cài Python trước!

## 🚀 Các trường hợp

### 1. Chưa có Python gì cả:
```bat
run.bat
```
→ Chờ 2-5 phút, tự động cài Python 3.11 và chạy!

### 2. Đã có Python 3.11 hoặc 3.12:
```bat
run.bat
```
→ Chạy ngay lập tức!

### 3. Đang dùng Python 3.14:
```bat
run.bat
```
→ Script tự động cài Python 3.11
→ Yêu cầu đóng CMD và mở lại
→ Chạy lại `run.bat`

## ⚠️ LƯU Ý QUAN TRỌNG

**Sau khi cài Python 3.11:**
1. **Đóng cửa sổ CMD hiện tại**
2. **Mở CMD mới**
3. Chạy: `run.bat`

(Windows cần reload PATH để nhận Python mới)

## 📱 Kết quả

- Cửa sổ desktop với giao diện Google Drive
- Auto-login từ Telegram Desktop (nếu đã đăng nhập)
- Upload/Download files qua Telegram
- Quản lý files như Google Drive

## 🆘 Nếu có lỗi

### Lỗi: "Python 3.11 installation failed"
```bat
install_python311.bat
```
Hoặc download: https://www.python.org/downloads/release/python-31110/

### Lỗi: "Please close and reopen CMD"
→ Đóng CMD này, mở CMD mới, chạy lại `run.bat`

### Lỗi khác
→ Xem file: `teledrive.log`

## 💡 Tips

- **Lần đầu**: 2-5 phút (download & install)
- **Lần sau**: Vài giây
- **Không cần tương tác** - để script tự chạy
- **Giữ Telegram Desktop mở** để auto-login

## 🎉 Xong!

```bat
run.bat
```

Ngồi chờ, uống cà phê ☕, mọi thứ tự động! 🚀

---

## 📂 Files quan trọng

- `run.bat` - **CHẠY FILE NÀY**
- `auto_install_python311.bat` - Auto install Python 3.11
- `CHAY_NGAY.md` - File này
- `teledrive.log` - Log file nếu có lỗi
